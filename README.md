# C_Study

```
git push -u origin main
分支在main上面

git clone https://github.com/raylee-lilei/C_Study.git

修改
git status

git config --global user.name "raylee-lilei"
git config --global user.email "751301185raylee@gmail.com"
git add .
git commit -m "数组和指针"
git push -u origin main

```